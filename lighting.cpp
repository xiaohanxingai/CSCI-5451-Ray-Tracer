#include "Include/lighting.h"

Color ApplyLighting(const Scene& scene,
                    const Ray &ray,
                    const HitInfo &hit,
                    int depth) {
    // TODO: Adrian
    return Color(0, 0, 0);
}
